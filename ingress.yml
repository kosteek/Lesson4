apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-ingress
spec:
  rules:
  - host: localhost
    http:
      paths:
      - backend:
        serviceName: myapp-service
        servicePort: 8088
      path: /
